##begin config
libbasetype struct TestBase
version 0.1
##end config

##begin cdef
#include <libraries/test.h>
#include <utility/tagitem.h>
##end cdef

##begin cdefprivate
#include "test_intern.h"
##end cdefprivate

##begin functionlist
struct TestSuite *CreateTestSuite(struct TagItem *tags) (A0)
BOOL RunTestSuite(struct TestSuite *ts) (A0)
void DestroyTestSuite(struct TestSuite *ts) (A0)
void RecordTestSuccess(struct TestSuite *ts) (A0)
void RecordTestFailure(struct TestSuite *ts) (A0)
void RecordTestFailureFileLine(struct TestSuite *ts, const char *file, int line) (A0,A1,D0)
void OutputExpectedLong(struct TestSuite *ts, LONG got, LONG expected) (A0,D0,D1)
void OutputExpectedString(struct TestSuite *ts, STRPTR got, LONG glen, STRPTR expected, LONG elen) (A0,A1,D0,A2,D1)
void OutputUnwantedLong(struct TestSuite *ts, LONG got) (A0,D0)
void OutputUnwantedString(struct TestSuite *ts, STRPTR got, LONG glen) (A0,A1,D0)
##end functionlist
